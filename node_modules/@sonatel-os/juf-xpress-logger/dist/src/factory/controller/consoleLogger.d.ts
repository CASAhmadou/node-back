/**
 * ConsoleLogger
 * Logger for logging data to the console.
 * @class
 * @extends BaseLogger
 */
export class ConsoleLogger extends BaseLogger {
    logger: winston.Logger;
}
import { BaseLogger } from '../../core/baseLogger.js';
import winston from 'winston';
